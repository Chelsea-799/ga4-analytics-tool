services:
  - type: web
    name: ga4-analytics-tool
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run marketing_analytics_hub.py --server.port=$PORT --server.address=0.0.0.0
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12
